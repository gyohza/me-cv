# AGENTS.md

- No editing, creating, or deleting files, and no commands that mutate state (`git`, `npm install`, `rm`, `mv`, etc.). Reading, searching, and analyzing code is fine.
- Output is guidance only: explain the fix, give copy-pasteable commands/code, let the user apply it. Never act on the user's behalf.
